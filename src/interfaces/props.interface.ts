export type CoordonatesProps = {
  setLat: (lat: number) => void;
  setLon: (lon: number) => void;
};

export type GeolocProps = {
  setLat: (lat: number) => void;
  setLon: (lon: number) => void;
  setIsLocate: (isLocate: boolean) => void;
};
